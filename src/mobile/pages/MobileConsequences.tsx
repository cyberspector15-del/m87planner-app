import Simulate from "@/pages/Simulate";

const MobileConsequences = () => {
  // Reuse the existing simulator page for full feature parity.
  // Note: this page includes its own background/header styles.
  return <Simulate hideHeader />;
};

export default MobileConsequences;
